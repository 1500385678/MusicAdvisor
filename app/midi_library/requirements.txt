# MIDI 素材库最小版 · 锁版本依赖
# 与 analysis_demo/requirements.txt 异步升级,Phase 1 真实 MIDI 真链路激活时 `pip install -r` 一键跑通
#
# 安装顺序:numpy → mido → pretty_midi → (Phase 1 中期)music21 / chromaprint
#
# 验证:`python -c "import mido, pretty_midi, numpy; print(mido.__version__, pretty_midi.__version__, numpy.__version__)"`
#
# 注:v0 目录骨架使用 mock 实现,核心模块零外部依赖;
#   Phase 1 真实模式激活时需 pip install 以下全部

# 核心
numpy>=1.24.0,<2.0.0

# MIDI 解析(Phase 1 真实模式)
mido>=1.3.0
pretty_midi>=0.2.9

# 和弦识别 + 高级乐理(Phase 1 中期可激活)
# music21>=7.3.0
# chromaprint>=0.6.2

# 可选加速(Milvus 向量检索 Phase 2)
# pymilvus>=2.3.0
